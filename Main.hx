using stx.Log;
using stx.Nano;
using stx.Ext;
using stx.async.Pack;
using stx.parse.Pack;

using eu.ohmrun.Pml;

class Main {
	static function main() {
		//var timer = __.timer();
		eu.ohmrun.pml.Test.main();
		//trace(timer.since());
	}
}